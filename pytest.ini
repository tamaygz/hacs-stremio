[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --allow-hosts=*
markers =
    asyncio: mark a test as an asyncio test
